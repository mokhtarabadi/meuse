# Example environment file for the Meuse git HTTP server
# Copy this file to `.env` and edit values before running `scripts/gen-htpasswd.sh`

GIT_USER=gituser
GIT_PASSWORD=ChangeMeStrongPassword
GIT_PORT=8180
